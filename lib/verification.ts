/**
 * When the seeded organisation records — registration details and official
 * donation URLs — were last checked against a primary source. The badge claims
 * exactly this and nothing more, so the date is rendered wherever it appears.
 */
export const VERIFICATION_CHECKED_AT = "2026-09-15";
